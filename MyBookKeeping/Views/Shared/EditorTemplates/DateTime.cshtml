@model DateTime?

@{
    var value = Model?.ToString( "yyyy-MM-dd" ) ?? "";
}

@Html.TextBox( "", value, new
{
    @class = "form-control",
    data_datepicker = "",
    data_date_format = "YYYY-MM-dd",
    placeholder = "請選擇日期"
} )